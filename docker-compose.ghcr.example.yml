services:
  api-niyyah2:
    image: ghcr.io/ferilee/api-niyyah2:latest
    container_name: api-niyyah2
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - .env
    networks:
      - ferileeNet

networks:
  ferileeNet:
    external: true
